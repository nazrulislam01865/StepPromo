@props(['job'])
<section class="ft-detail-card"><h2>Commercial</h2><div class="ft-overview-metrics"><div class="ft-overview-metric"><span class="ft-metric-icon red">$</span><div><small>Commercial value</small><b>{{ $job->commercial_value > 0 ? $job->currency.' '.number_format($job->commercial_value,0) : 'Quotation pending' }}</b><p>{{ $job->commercial_value > 0 ? 'Recorded' : 'Not recorded yet' }}</p></div></div><div class="ft-overview-metric"><span class="ft-metric-icon blue">▣</span><div><small>Invoice status</small><b>{{ $job->commercial_value > 0 ? 'Draft' : 'Not created' }}</b><p>{{ $job->phase?->short_name }}</p></div></div><div class="ft-overview-metric"><span class="ft-metric-icon purple">⌘</span><div><small>Client balance</small><b>{{ $job->currency }} {{ number_format($job->client?->outstanding_balance ?? 0,0) }}</b><p>{{ $job->client?->name }}</p></div></div></div></section>
