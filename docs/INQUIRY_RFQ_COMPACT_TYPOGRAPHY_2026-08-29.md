# Inquiry RFQ Compact Typography — 2026-08-29

The Inquiry Details > RFQ product workspace was tightened after the central-typography pass so it matches the density of surrounding FlowTrack detail sections.

The implementation continues to use only centralized FlowTrack typography tokens. No one-off numeric font sizes or font weights were introduced.

Compact changes include smaller card/icon dimensions, reduced vertical padding, tighter product headers, 34px supplier rows, smaller action controls, a tighter footer/pagination area, and use of the central `xs`/`sm` scale for dense table content while retaining the central `lg` section heading scale.

No RFQ data, invitation, quotation, retry, supplier-assignment, filtering, pagination, or reusable-component behavior was changed.
