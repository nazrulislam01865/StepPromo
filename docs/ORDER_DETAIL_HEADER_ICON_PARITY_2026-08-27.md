# Order detail header icon parity — 2026-08-27

Updated the Order details header metadata to use the same clean line-icon language as the Inquiry details header.

## Changes
- Replaced legacy Unicode glyphs in Order metadata with inline SVG icons.
- Client uses the shared client logo component plus the user/client icon treatment.
- Reference uses the document icon and now has a compact copy action.
- Created by uses the user icon.
- Created uses the calendar icon.
- Order ID copy action now uses the same copy-style line icon.
- Added responsive handling so metadata wraps cleanly on smaller screens.

No business logic, workflow behavior, permissions, or database schema were changed.
